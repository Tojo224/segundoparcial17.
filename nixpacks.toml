[phases.setup]
nixPkgs = ['php83', 'php83Packages.composer', 'nginx', 'nodejs_20']

[phases.build]
cmds = [
  'composer install --no-interaction --prefer-dist --optimize-autoloader --no-scripts'
]

[start]
cmd = 'php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT'